# Root reference only. Copy the relevant values into `server/.env` and `client/.env`.

# Server
PORT=5001
JWT_SECRET=replace_this_with_a_long_secret_key
MONGO_URI=
CLIENT_URL=http://localhost:5173

# Client
VITE_API_URL=http://localhost:5001/api
